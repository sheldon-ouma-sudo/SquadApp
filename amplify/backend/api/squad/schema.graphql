type User @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  userName: String
  imageUrl: String
  userSquadId: String
  numOfPolls: Int
  numOfSquadron: Int
  userInterests: [String]
} 